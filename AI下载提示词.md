# AI 下载提示词（给任意 AI 助手用）

把下面整段内容复制、粘贴给任意 AI 助手（ChatGPT / Kimi / 豆包 / 文心一言 / 通义千问等），
它就能照着帮你完成「筛股」软件的下载、安装与首次数据同步。

---

```text
请帮我安装一个叫「筛股 Screengu」的 A 股量化选股软件，以下是它的官方说明，请严格照着执行并逐步引导我。

【软件简介】
「筛股 Screengu」是一个跑在本地电脑上的 A 股选股工作台，用 FastAPI + 原生前端开发。
包含：6 大经典策略选股、13 条件筛选、形态打分、蚂蚁呀欸四层扫描、超长周期选股、
相似股、行情查看、前瞻预警、大周期分析、快讯轮播、分红时点预测、B站 UP主内容挖掘等模块。
所有数据按需联网抓取、只存在本机 SQLite，不上传服务器。

【运行环境】
- 操作系统：Windows（当前版本一键启动仅支持 Windows）
- 依赖：Python 3.11 或更新（3.12 / 3.13 均可）
- 其余依赖（FastAPI / pandas / AKShare 等）由程序自动安装，用户无需手动配置

【下载方式】
从 GitHub Release 页面下载源码包（zip），解压到任意英文路径目录（路径中不要含中文或空格，避免兼容问题）。

【安装与启动】
1. 确认已安装 Python 3.11+，安装时勾选 "Add Python to PATH"。
2. 进入解压后的目录，双击 start.bat。
3. 程序会自动 pip 安装全部依赖（首次约 1~3 分钟），随后自动打开浏览器进入工作台。
4. 若浏览器未自动打开，手动访问 http://127.0.0.1:8765/ 。

【首次数据同步（必须联网，约 30 分钟，仅一次）】
1. 网页右上角点「刷新信息」拉取全市场股票列表（约 1 分钟）。
2. 点「同步数据」拉取日线/估值/分红（可先选小范围试跑）。
3. 之后每次点「⬇ 同步数据」即增量更新，只需几分钟。

【常见问题排查】
- 双击 start.bat 后一闪而过 / 报错：多半是没装 Python 或没勾选 Add to PATH，请先装 Python。
- 端口被占用：start.bat 会自动检测并直接打开浏览器，或手动指定其他端口启动。
- 直接双击 index.html 打开会显示红色提示、功能不可用：必须通过 start.bat 启动。
- 依赖安装失败：可手动执行 `pip install -r requirements.txt` 后重试。

【重要声明】
- 本软件代码为 MIT 开源，仅供学习研究使用，不构成任何投资建议。
- 部分表情包/动图为网络或游戏素材，版权归原作者，严禁商用。
- 数据来自免费公开渠道，仅供学习研究，请勿商用或大规模再分发。
```

---

（附）更省事的做法：把 `README.md` 直接拖给 AI 助手，说「照这份文档帮我装好」，效果一样。
